# Local development: leave empty to use Vite proxy (/api -> backend)
VITE_API_BASE_URL=

# Production Docker build (set via --build-arg at image build time):
# VITE_API_BASE_URL=http://<YOUR-ALB-DNS-HERE>
